import { AuthLoading } from "@/components/route-loading"

export default function Loading() {
    return (
        <AuthLoading
            title="Preparing account registration"
            description="Loading secure onboarding and account setup controls."
        />
    )
}