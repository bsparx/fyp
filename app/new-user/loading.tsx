import { RedirectLoading } from "@/components/route-loading"

export default function Loading() {
    return (
        <RedirectLoading
            title="Setting up your account"
            description="Finalizing your profile and preparing the dashboard redirect."
        />
    )
}