import { AuthLoading } from "@/components/route-loading"

export default function Loading() {
    return (
        <AuthLoading
            title="Opening sign in"
            description="Connecting to secure authentication services."
        />
    )
}